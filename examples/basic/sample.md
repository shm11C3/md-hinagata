---
hinagata:
  theme: basic
  output: fragment
---

# Welcome to **md-hinagata**

Write **Markdown**, choose a theme, and copy `controlled HTML`.

## Inline **Markdown** in templates

This paragraph keeps **strong text** and `inline code` because `p.hbs` uses `inner_html`.

## Code example

```ts
const message = "Hello, world!";
console.log(message);
```

```html
<strong>escaped</strong>
```
